# GitHub Actions マニュアル

このマニュアルでは、Ananke プロジェクトで使用している GitHub Actions について説明します。

## GitHub Actions とは

GitHub Actions は、GitHub が提供する CI/CD（継続的インテグレーション / 継続的デリバリー）サービスです。リポジトリへのプッシュやプルリクエストをきっかけに、テストやビルドなどの処理を自動で実行できます。

### 基本的な用語

| 用語 | 説明 |
| --- | --- |
| **ワークフロー** | `.github/workflows/` に置かれる YAML ファイル。自動化の一連の処理を定義します |
| **トリガー（`on`）** | ワークフローを実行するきっかけ（プッシュ、プルリクエスト、手動実行など） |
| **ジョブ（`jobs`）** | ワークフロー内の実行単位。仮想マシン上で動きます |
| **ステップ（`steps`）** | ジョブ内の個々の処理。アクションの呼び出しやコマンドの実行を行います |
| **アクション（`uses`）** | 再利用可能な処理の部品。公式やコミュニティが提供しているものを利用できます |

### このプロジェクトでの使い方

```
プッシュ or プルリクエスト
        │
        ▼
┌───────────────────────┐
│  GitHub Actions       │
│                       │
│  1. コードを取得       │
│  2. Dev Container 起動 │
│  3. npm install       │
│  4. npm run build     │
│         │             │
│    成功 / 失敗         │
└───────┬───────────────┘
        │
        ▼
  結果を GitHub に表示
```

ビルドが失敗すると、プルリクエストにエラーが表示されるため、問題を素早く発見できます。

## ワークフロー一覧

| ワークフロー                                      | ファイル                     | 説明                            |
| ------------------------------------------- | ------------------------ | ----------------------------- |
| [Test Dev Container](setup-develop-test.md) | `setup-develop-test.yml` | Dev Container 内でビルドが通るかを検証します |

## セキュリティ

このプロジェクトでは、GitHub Actions のセキュリティを高めるためにいくつかの対策を行っています。詳しくは[セキュリティ対策](security.md)を参照してください。
